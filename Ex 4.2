#Reversing a part of a list in place, reverse_range(L, left, right), the right index is included. Write iterative and recursive versions.
#wersja iteracyjna

def reverse(seq, start, stop):
    size = stop + start
    for i in range(start, (size + 1) // 2):
        j = size - i
        seq[i], seq[j] = seq[j], seq[i]

        
reverse(L, 2,5)
print (L)

#wersja rekurencyjna
def reverse_range(L, left, right):
    if left < right:
        L[left], L[right] = L[right], L[left]
        reverse_range(L, left+1, right-1)

        
reverse_range (L, 3,5)
print(L)
